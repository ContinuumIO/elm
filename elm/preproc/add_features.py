

def add_features(sample, **add_features_config):
    '''Inputs:
        sample: a Sample namedtuple: (df, band_meta, filemeta, filename)
        kwargs:

       Returns dataframe with new columns
    '''

